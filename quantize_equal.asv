function y = quantize_equal(x,xmin,xmax,n_steps)
    step_size = (xmax-xmin)/n_steps;
    
    %normalize data to be between 0 and 1
    x_norm =(x-min)/(xmax-xmin);
    floor(x_norm*n_steps);
    
end

